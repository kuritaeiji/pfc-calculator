setup:
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false
build:
  docker:
    web: Dockerfile
run:
  web: yarn start

# herokuはベータ版にして、heroku plugins:install @heroku-cli/plugin-manifestでプラグインを追加
